import EnVivo from './EnVivo';

export const metadata = {
  title: 'En vivo · Ranked',
  description: 'Resultados y posiciones del torneo de frontón en tiempo real',
};

export default function Page() {
  return <EnVivo />;
}
